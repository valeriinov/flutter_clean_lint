name: flutter_clean_lint
description: "Custom lints for Flutter"
version: 0.0.1
homepage:

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  analyzer: '>=6.4.0 <7.0.0'
  analyzer_plugin:
  custom_lint_builder: ^0.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
