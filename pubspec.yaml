name: flutter_clean_lint
description: "Custom lints for Flutter"
version: 0.1.4
homepage:

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  analyzer: ^7.4.4
  analyzer_plugin:
  custom_lint_builder: ^0.7.6
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
